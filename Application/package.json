{
  "name": "tarmoapi",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "babel-node": "babel-node",
    "start": "npm run build && node dist/app.js",
    "build": "babel ./ --out-dir dist/ --ignore ./dist,./node_modules,./.babelrc,./npm-debug.log --copy-files",
    "dev": "nodemon --exec npm run babel-node -- ./app.js"
  },
  "description": "TarmoAPI",
  "author": {
    "name": "datacodex"
  },
  "dependencies": {
    "body-parser": "^1.15.0",
    "cookie-parser": "^1.4.0",
    "debug": "^2.2.0",
    "express": "^4.14.0",
    "morgan": "^1.7.0",
    "natives": "^1.1.6",
    "pug": "^2.0.0-beta6",
    "serve-favicon": "^2.3.0"
  },
  "engines": {
    "node": "~6.10.x"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
